# 原 foxbook PHP 后端接口统计

> 统计对象：`foxbook-master/foxbook-php` 后端（webman 框架）

## 统计口径

- **范围**：`app/api/controller/`（用户端 `/api`）与 `app/adminapi/controller/`（管理端 `/adminapi`）下的所有具体控制器。
- **计数单元**：webman 默认路由下，每个 `public` 方法 = 1 个可访问接口 action。
- **继承计入**：所有控制器继承 `app\BaseController`，其自带通用方法 `getListByPage` / `getList` / `getInfo` 会被子类自动拥有并计入（除非子类重写，重写只算一次）。
- **排除项**：响应辅助方法 `success` / `fail`，以及构造/`__magic` 方法，不计入业务接口。
- **同名分离**：`api` 与 `adminapi` 两个模块下存在同名控制器（如 `Post`、`User`、`Auth`），因 namespace 不同、职责不同，分别独立计数。
- **PC 页面路由**：`config/route.php` 中的 `Route::` 定义为前端页面路由（返回 HTML），非 API，单列作参考。

## 总览

| 模块 | 控制器数 | 接口数 |
|---|---:|---:|
| `/api` 用户端 | 13 | 115 |
| `/adminapi` 管理端 | 14 | 130 |
| **合计（API）** | **27** | **245** |
| `config/route.php` 页面路由 | — | 32（非 API） |

## `/api` 用户端明细（13 控制器 / 115 接口）

| 控制器 | 接口数 | 方法 |
|---|---:|---|
| Auth | 25 | accountLogin, accountRegister, changePassword, deleteMenus, deleteRole, deleteUser, getAllMenus, getInfo, getList, getListByPage, getMenus, getParentMenus, getRoleMenus, getRoles, getUsers, login, logout, mnpLogin, saveMenus, saveRole, saveRolePermission, saveUser, sendEmailCode, updateProfile, uploadAvatar |
| Post | 16 | batchDeletePosts, currentUser, deletePost, favorite, favorites, favoritesByUserId, getInfo, getList, getListByPage, getPostDetail, like, likes, likesByUserId, publish, setTop, wechatCheckImg |
| User | 14 | checkFollowStatus, deleteUser, follow, getHotUsers, getInfo, getList, getListByPage, getUserById, saveInfo, saveUser, setAuthTags, toggleUserStatus, unfollow, updateProfile |
| Category | 9 | batchDeleteCategories, createCategory, deleteCategory, getCategoryDetail, getCategoryOptions, getInfo, getList, getListByPage, updateCategory |
| Link | 7 | batchDeleteBanners, deleteBanner, getBannerDetail, getInfo, getList, getListByPage, save |
| Message | 7 | getInfo, getList, getListByPage, getUnreadCount, getUserMsgList, markAllAsRead, markAsRead |
| PlatformConfig | 6 | getInfo, getList, getListByPage, getPlatformConfig, getPublicSiteConfig, saveConfig |
| ActivityMessage | 6 | getDetail, getInfo, getList, getListByPage, save, searchUsers |
| Agreement | 5 | getAgreement, getInfo, getList, getListByPage, saveAgreement |
| Comments | 8 | getChildComments, getInfo, getList, getListByPage, getParentComments, like, publish, toggleLike |
| Discuss | 4 | getInfo, getList, getListByPage, publish |
| Search | 4 | getInfo, getList, getListByPage, query |
| WechatMp | 4 | getInfo, getList, getListByPage, wxService |

## `/adminapi` 管理端明细（14 控制器 / 130 接口）

| 控制器 | 接口数 | 方法 |
|---|---:|---|
| Auth | 25 | （同 `/api/Auth` 方法列表，职责为管理端 RBAC/用户管理） |
| Post | 16 | （同 `/api/Post`，职责为帖子管理） |
| User | 14 | （同 `/api/User`，职责为用户管理） |
| Category | 9 | （同 `/api/Category`，职责为类目管理） |
| Comment | 9 | batchDeleteComments, createComment, deleteComment, getCommentDetail, getInfo, getList, getListByPage, updateComment, updateCommentStatus |
| Link | 7 | （同 `/api/Link`，职责为轮播管理） |
| PlatformConfig | 6 | （同 `/api/PlatformConfig`） |
| ActivityMessage | 6 | （同 `/api/ActivityMessage`） |
| Agreement | 5 | （同 `/api/Agreement`） |
| AuthTag | 6 | deleteTag, getInfo, getList, getListByPage, save, toggleStatus |
| SensitiveWord | 6 | deleteWord, getInfo, getList, getListByPage, save, toggleStatus |
| WebTopNav | 7 | batchDeleteNavs, createNav, deleteNav, getInfo, getList, getListByPage, updateNav |
| Dashboard | 10 | getInfo, getList, getListByPage, getPostStatistics, getRecentLogs, getStatistics, getSystemInfo, getUserDistribution, getUserGrowth, getUserStatistics |
| OperationLog | 4 | clearLogs, getLogDetail, getStatistics, process |

## 与 Java（zbtech）翻译进度对照

- **已翻译（Java 已实现对应逻辑）**：`/api` 的 Auth(5)、Post(10)、Comments(4)、Message(4) ≈ 23 / 115；`/adminapi` 的 Comments 删除已覆盖级联逻辑。
- **待翻译**：`/api` 剩余约 92 个接口 + `/adminapi` 剩余约 130 个接口（管理端业务含 Dashboard 统计、OperationLog 日志、SensitiveWord 敏感词、AuthTag 标签、WebTopNav 导航、各资源增删改等）。

> 注：`api` 与 `adminapi` 下同名控制器在统计中各算一套（原项目即为两个独立模块）；Java 翻译时 `/api` 侧重用户端业务，`/adminapi` 侧重后台管理，可分别落地。
